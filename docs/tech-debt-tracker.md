---
owner: flash-urbano
status: living
last_reviewed: 2026-08-02
update_trigger: on-debt-added-or-resolved
---

# Tech debt tracker

Running ledger of known hazards, work-in-progress shortcuts, and deferred
improvements. Maintained per the harness steering loop (see
[`processes/harness.md`](processes/harness.md)).

## How this works

Each row is one item. New items go at the top of the relevant section.

- **Severity.** `High` (active hazard or correctness risk), `Medium` (hurts
  velocity or clarity), `Low` (cosmetic or speculative).
- **Status.** `Open`, `In progress`, `Resolved`, `Dropped`.
- **Description.** What the debt is, in one sentence.
- **Resolution.** How it was paid, with a commit/plan/ADR reference where
  useful. Empty while `Open`.

## Open

| Date identified | Severity | Description |
|---|---|---|
| 2026-09-11 | Low | **Seis pasos del quickstart de `025` sin confirmar.** Mateo valido en el navegador lo central: el tablero completo con su cuenta admin, y que una cuenta comun no lo ve. Los numeros ya estaban verificados contra la base sin navegador (tasks T032). **No reportados**: Q8 (dar de baja un pedido baja el total y la pantalla no parece rota), Q9 (ningun `$` en ningun corte), **Q11** (entrar SIN recargar: desde el panel de `/tablero`, desde `/ingresar` —el enlace "Tablero" tiene que aparecer solo—, y con la cuenta comun), Q12 (con `/admin/tablero` bloqueado, error y *Reintentar*, nunca ceros), Q13 (base vacia) y Q14 (360 px). **El que importa es Q11**: es el cruce que encontro el analyze (C1) —el ingreso no trae `esAdmin`, solo `/yo`— y **si Mateo ya tenia la sesion abierta al probar, ese camino no se recorrio**: el usuario venia de `/yo` y traia el campo. Su falla es visible y no expone nada —Diego veria "solo para la administracion" o no veria el enlace hasta recargar—, porque el que niega de verdad es el servicio. **Como se paga**: salir, abrir `/tablero` y entrar ahi mismo; despues salir y entrar por `/ingresar`. Sin recargar en ningun momento. **Disparador**: antes de mostrarle el tablero a Diego, o la proxima vez que se toque `proveedor-sesion.tsx`. |
| 2026-09-11 | Low | **El tablero de `025` manda una fila por pedido, sin paginar ni agregar, y eso tiene techo.** `GET /admin/tablero` devuelve todos los pedidos de la base —instante de carga, cantidad y cuenta— y la web agrupa en `lib/tablero.ts` (research D1 de `025`). Se eligio asi porque la zona horaria y la semana quedan con una prueba que corre siempre, y porque a este volumen no hay nada que ganar agregando en SQL. **Cuanto es el techo**: ~100 bytes por fila, o sea **~1 MB sin comprimir a 10.000 pedidos**; hoy son 14. Tambien crece el corte por dia, una fila por dia desde el primer pedido (~365 por año), que es una tabla que se desplaza y no se rompe. **Como se paga**: mover el agrupado a SQL (`date_trunc` con `AT TIME ZONE 'America/Montevideo'`, lunes como inicio de semana) y cambiar el cuerpo de la respuesta, **no la pantalla**: `resumir()` pasa a recibir filas ya agrupadas. Y portar a Go las pruebas de `lib/tablero.test.ts`, con el proceso en otra zona como hace hoy la de vitest. **Disparador**: la respuesta pasa de ~500 KB, o el tablero tarda en abrir desde la computadora de Diego. |
| 2026-09-10 | Low | **RESUELTA EL MISMO DIA, y mejor de lo esperado.** SC-008 se demostro sin depender de la fecha: en `flash-pg-dev` hay **6 pedidos con `zona_id = 1` y `precio = 200`**, creados el 2026-09-01 — y la zona 1 vale **150**; 200 es el precio de la zona 2. O sea que la columna guarda un numero que no corresponde a la zona del pedido, **en filas POSTERIORES al cambio de retiro→entrega del 2026-08-22**, asi que la ficcion no es solo un problema de pedidos viejos. Es exactamente por esto que el Principio V prohibe leerla y que `024` no la lee en ningun camino. El contraste positivo tambien quedo hecho: el pedido de prueba del 2026-09-10 (`FU-0015`) guardo `zona_id = 1, precio = 150`, que es lo que mostro la pantalla (SC-004). **No se investigo POR QUE esas seis filas tienen 200** —pueden ser datos sembrados a mano o un camino viejo del formulario—, y no hace falta para este feature: nadie las lee. Si alguna vez se propone mostrar el precio guardado, empezar por acá. Texto original abajo. // **SC-008 de `024` no se puede verificar en la base local: no hay ningun pedido anterior al 2026-08-22.** El criterio dice que un pedido creado antes de esa fecha —cuando el monto se calculaba desde la zona de RETIRO y no de entrega— no muestra ese numero guardado en ninguna parte. Es el caso que justifica que la columna `precio` siga prohibida. Se midio el 2026-09-10 contra `flash-pg-dev`: **12 pedidos, el mas viejo del 2026-08-31, cero previos al 2026-08-22.** **Que cuesta**: poco hoy, porque el feature no lee esa columna en ningun camino y FR-011 se cumple no tocando `historial.tsx` — pero si alguien mas adelante decide mostrar el precio de un pedido viejo, este es el caso que le va a explotar y no hay dato local con que darse cuenta. **Como se paga**: insertar a mano un pedido con `creado_en` anterior al 2026-08-22 y un `precio` que no corresponda a su zona de entrega, y correr el paso 9 del quickstart. **Disparador**: si alguna vez se propone leer la columna `precio` para mostrarsela a alguien. |
| 2026-09-10 | Low | **RESUELTA EL MISMO DIA: los 11 pasos del quickstart de `024` corridos.** El paso 10 cerro con una correccion del SPEC y no del codigo: decia que con el servicio caido y credencial guardada el monto se seguia viendo, y es imposible — `proveedor-sesion.tsx` rehidrata llamando a `/yo`, esa llamada falla, el `catch` a proposito no borra la credencial pero tampoco confirma al usuario, y sin usuario confirmado no hay monto (FR-005a). **Falla hacia el lado seguro** y no cuesta nada, porque con el servicio caido tampoco se puede crear el pedido. SC-006 quedo reescrito. Texto anterior de esta fila abajo. // Quedaba **solo el paso 10** —que con el backend abajo y credencial guardada el formulario cargue y el monto se siga viendo—. **Riesgo bajo y con media verificacion automatica**: `web/lib/cotizar-abierto.test.ts` garantiza estaticamente que el grafo de imports del formulario no llega a `lib/api.ts` ni a `lib/sesion.ts`, y esta verde; el monto se calcula en el navegador desde `lib/zonas.ts`, sin red. Lo que no esta comprobado a mano es el conjunto. **Como se paga**: `docker stop flash-pg-dev` o cortar el `go run`, y recargar `/pedido` con sesion ya guardada. **Disparador**: la proxima vez que se toque el formulario o el proveedor de sesion. Texto original abajo. // **Los once pasos del quickstart de `024` estan sin correr, y son lo UNICO que ve si el feature existe.** El codigo esta hecho y `verify:` esta verde: lint limpio, 224 pruebas, build OK, y las cuatro guardas se rompieron a proposito y se vieron en rojo. **Nada de eso prueba el feature.** `web/` corre vitest en `environment: node` y no renderiza React, asi que ninguna prueba automatica puede abrir el formulario y mirar si hay un monto. **Que cuesta**: el modo por defecto de este feature es el correcto — si `conSesion` llegara siempre `false`, el sitio se veria exactamente como hoy, las tres patas del `verify:` pasarian igual, y el feature no existiria sin que nada avise. El caso simetrico es peor: si el monto se filtrara a un visitante sin sesion, eso es lo unico que el cliente pidio que no pasara, y tampoco hay prueba que lo vea. Los pasos mas filosos son el 6 (la ventana en que la sesion no se resolvio, con la red desacelerada), el 7 (entrar por el dialogo a mitad de formulario sin perder lo tipeado — ese camino ya rompio el formulario el 2026-08-14 con T039) y el 2b (contrastar el monto de la pantalla contra la columna `precio` de la base). **Como se paga**: `cd web && npm run dev` y los once pasos de `specs/024-precio-detras-del-login/quickstart.md`, que no necesitan telefono. **Disparador**: antes de mergear la rama `024-precio-detras-del-login`. El plan queda en `status: active` a proposito hasta entonces. |
| 2026-09-06 | Low | **SC-001 de `023` —el unico criterio del feature que se mide con un reloj— nunca se midio, porque no habia con que.** El criterio es: con **50 pedidos** cargados, alguien encuentra uno concreto en **menos de 15 segundos**. El quickstart se corrio con una cuenta de una docena, y ahi la lista entra en pantalla y encontrar no cuesta nada: medirlo con ese volumen no dice absolutamente nada sobre el problema que el feature existe para resolver. **Que cuesta**: si los controles no alcanzan con 50, nadie se va a enterar hasta que un cliente real llegue a ese numero, y para entonces el feature ya se dio por bueno. **Como se paga**: cargar ~50 pedidos contra la base local con un script y correr el paso 9 del quickstart con alguien que no construyo la pantalla. **Disparador**: cuando el cliente con mas envios pase de 30 pedidos, o antes si se quiere hacer bien. |
| 2026-09-06 | **Resuelta en parte el 2026-09-06** | **LOS AVISOS LLEGAN.** Mateo lo confirmo con `022` ya desplegado: edito pedidos reales y **el push llego siempre**. Eso cierra lo que esta fila tenia de urgente —el paso 6 del quickstart, el que decidia si el feature entregaba menos que antes de existir— y su disparador quedo cumplido. **Sigue sin correrse** lo demas del quickstart: la revalidacion de cobertura al editar la entrega (paso 3), **la carrera de FR-012** (paso 5, tomar el pedido entre medio: necesita dos manos a la vez y no se da sola) y que la app tolere un pedido que dejo de existir (paso 7). Ninguna de las tres tiene el filo de la de los avisos, pero tampoco las cubre ninguna prueba. Texto original abajo. // **`022` cerro con la mitad del quickstart sin correr, y lo que queda afuera es todo lo que pasa del lado de Diego.** Lo que **si** se probo a mano el 2026-09-06, en el navegador y contra el backend local: editar precarga el pedido entero —incluida la fecha—, guardar funciona, dar de baja funciona, y el preflight de PATCH/DELETE se midio con `curl` contra el servicio. Lo que **no** se corrio: el paso 3 (que editar la entrega **revalide la cobertura** y no deje guardar fuera de zona), el paso 5 (la ventana cerrada y **la carrera de FR-012** — tomar el pedido entre medio), el paso 6 (**los dos avisos nuevos a Diego en un telefono de verdad**) y el paso 7 (que la app tolere un pedido que dejo de existir). **Por que importa el 6**: el aviso es lo unico que le dice a Diego que un pedido cambio; si no llega, sale a buscar un paquete a una direccion vieja, y el feature entrega **menos** que antes de existir. **Por que no se corrio**: el APK instalado apunta a produccion y el pedido de prueba vivia en la base local, asi que el paso 6 recien se puede correr con esto desplegado. **Disparador**: la primera edicion real despues del merge tiene que producir un push en el telefono de Diego. Si no llega, es esta fila. |
| 2026-09-05 | **High** | **El `verify:` de cinco planes falla si las pruebas de base realmente corren, y nadie lo noto porque por defecto no corren.** Medido el 2026-09-05 sobre `master` sin ningun cambio de codigo: con `flash-pg-test` levantado y `TEST_DATABASE_URL` puesta, `go test ./...` **falla** en `internal/auth` y `internal/pedidos` (`TestElEmailNoDistingueMayusculas`, `TestUnPedidoNuevoAvisaUnaVez`, `TestUnReintentoConLaMismaClaveNoAvisaDosVeces`, `TestLaMismaClaveDaDoscientosYElMismoPedido`). **No es un defecto del codigo**: con `go test ./... -p 1` todo queda en verde. Go corre los PAQUETES en paralelo por defecto y todos comparten **una sola base de pruebas**, asi que se pisan entre si. **`006` y `007` tenian `-p 1` en su `verify:`; se perdio en `011` y no volvio** — lo arrastran `011`, `012`, `016`, `017` y `018`. **Por que paso desapercibido, y es lo grave**: sin `TEST_DATABASE_URL` las pruebas contra Postgres **se saltean solas y todo dice `ok`**. Medido: **118 pruebas salteadas** en silencio contra **2** con la variable puesta. O sea que el `verify:` de esos planes estuvo verde sin haber tocado la base ni una vez, y el dia que alguien la levanta —para verificar de verdad— se encuentra con cuatro rojos que parecen suyos y no lo son. **Como se paga**: reponer `-p 1` en el `verify:` de los planes vivos y, mejor, que la ausencia de `TEST_DATABASE_URL` sea ruidosa en vez de silenciosa —un aviso al final de la corrida, o un objetivo de `verify:` que falle si el conteo de salteadas supera un umbral—. `021` ya lo lleva en su `verify:`. **Disparador**: el proximo feature que toque el backend, o antes si alguien reporta rojos que no puede explicar. |
| 2026-09-05 | Medium | **La etiqueta imprimible de `020` nunca se imprimio ni se abrio en un telefono, y las dos cosas que faltan son justo las que no se pueden deducir de la pantalla.** El feature esta entregado y probado end to end contra el backend local —se genera desde la confirmacion y desde Mis pedidos, con tildes y ñ reales, y el mensaje de error aparece con la red cortada—, pero **se miro en pantalla, no en papel**. **(a) T018, el papel**: la marca va como silueta negra porque el logo no se puede usar sobre blanco, y lo que no se sabe es si esa silueta sale como un camion o como una mancha en una impresora monocromatica con poco toner; tampoco si el `FU-####` a 46 pt se lee parado frente a una caja, que es el uso entero de la hoja. **(b) T023, el telefono**: **la descarga programatica se comporta distinto en cada navegador movil** —en iOS puede abrir un visor en vez de guardar—, y por FR-017 las tres variantes son correctas: lo que hay que comprobar no es donde aterriza sino **que llegue y se abra**. El sitio es mayoritariamente movil (Principio IV), asi que es la verificacion con mas chance de sorprender. **Como se paga**: los pasos 3 y 6 del quickstart de `020`, diez minutos con una impresora y un telefono. **Disparador**: antes de que un cliente real imprima la primera etiqueta. Si la silueta no se lee, se ajusta el tamaño en `etiqueta-pdf.ts` o se regenera con otro recorte — no se vuelve al logo a color, que sobre papel blanco pierde la mitad de sus elementos. |
| 2026-09-05 | **High** | **El borde de zonas que se desplego en `019` no lo confirmo el cliente, y la constitucion exige que lo confirme.** El texto es literal: *"Boundaries the client has not confirmed MUST NOT reach production"*. Diego pidio "modificar mapa zona 5"; **Mateo dibujo las coordenadas** en el KML, y el trazado resultante **toca dos poligonos, no uno**: gana ~0,21 km2 de cobertura nueva al este (zona 5 donde antes no habia nada) y **mueve ~0,34 km2 de zona 1 a zona 5**, que es mas de lo que el pedido nombraba. `019` lo tenia previsto: T015 exigia mostrarle el mapa y **bloqueaba el merge**, porque en este repo el merge ES el despliegue (`deploy-pages.yml` dispara con push a `master` filtrado por `web/**`). **Esa condicion se levanto a proposito el 2026-09-05, por decision de Mateo**, con el argumento de que no hay pedidos reales todavia y el costo de un borde equivocado es cero hoy. El argumento es correcto **mientras siga siendo cierto**: la exposicion es cero solo hasta el primer pedido real cerca de ese borde. **Sin hacer tambien queda T009**, la mitad de FR-006 que ninguna prueba cubre — que una entrega fuera de toda zona no deje confirmar y encamine al contacto, sin nombrar una zona cercana ni mostrar monto. **Como se paga**: los pasos 3 y 4 del quickstart de `019`, que son diez minutos con el sitio levantado. **Disparador: antes de promocionar el sitio o de que entre el primer pedido real**, lo que ocurra primero. Si el borde esta mal, se corrige el KML y se regenera —nunca se edita `lib/zonas.ts` a mano—, pero para entonces el arreglo ya no es gratis. |
| 2026-09-05 | Medium | **La decision de cobertura desempata leyendo una tabla de precios que el producto ya no publica.** Cuando dos zonas contienen el mismo punto, `resolverZonaEntre` (`web/lib/zona-lookup.ts`) elige la de **menor `precio`**, leyendo el campo que `build-zonas.js` escribe en `web/lib/zonas.ts`. Esa regla es una respuesta del cliente del 2026-08-06 y sigue siendo correcta; lo raro es que desde `013` **el producto no muestra precios en ninguna parte**, asi que el campo del que depende la admision de un pedido no tiene ya ningun otro uso visible. **Por que importa**: es exactamente la forma de un campo que alguien borra por "limpieza" —parece muerto— y borrarlo **cambia en silencio que zona se le asigna a un punto de borde**. No es la columna que el Principio V prohibe leer: esa es `pedidos.precio`, el monto guardado con cada pedido, y esta es la tabla de precios por zona. Que sean dos cosas distintas con el mismo nombre es la mitad del problema. **Un sintoma menor de lo mismo**: el comentario de cabecera de `web/lib/zona-lookup.test.ts` sigue diciendo que la funcion "decide cuanto se le cobra a una persona" y que un borde mal resuelto "se ve en la factura" — mundo anterior a `013`. **Como se paga**: renombrar el criterio a lo que hoy significa (una prioridad de zona que hoy coincide con el orden de precios) o dejar el campo con un comentario que diga que la cobertura lo lee. **Disparador**: el proximo feature que toque `zonas.ts`, `zona-lookup.ts` o el generador. Detectado escribiendo `019`; ver `specs/019-zona-5-corregida/research.md` D3 y D4, donde el comentario equivocado del generador ya indujo un error en un spec antes de corregirse. |
| 2026-09-01 | Medium | **La app guarda su token de avisos pero no lo declara hasta la siguiente llamada al servicio, y eso deja muda una instalacion nueva por un rato impredecible.** `onNewToken` lo escribe en el `DataStore`; el valor viaja en la cabecera `X-App-Push-Token` de la proxima peticion, que en una instalacion limpia puede tardar en ocurrir: la app pide la lista **al arrancar**, y el token del proveedor llego **dos minutos y medio despues** (medido el 2026-09-01). Si Diego abre la app, mira la lista y la cierra, el servicio no tiene a donde mandarle nada hasta que la vuelva a abrir. **Se cierra solo** —cualquier ingreso o *Actualizar* posterior lo declara— pero mientras dura, el sintoma es exactamente el que este feature existe para evitar, y en el registro se lee `el pedido FU-#### no tiene a quien avisarle`, que manda a mirar el telefono cuando el problema es de sincronizacion. **No fue la causa del episodio del 2026-09-01** —ese fue el permiso de notificaciones— y por eso no se arreglo en caliente. **Como se paga**: en el callback que guarda el token, disparar una recarga de la lista, que es la llamada que lo lleva. Son dos lineas en `RepartidorViewModel`. **Disparador**: la proxima version que se publique, o el primer "no me llego" cuyo registro diga que no habia a quien avisarle. |
| 2026-09-01 | Medium | **Tres verificaciones de `018` quedan sin correr, y dos de ellas no se pueden correr el mismo dia que se escriben.** El plan se cierra igual porque el feature esta **entregado, desplegado y comprobado en un telefono de verdad** —el aviso llega, con el texto correcto, y un reintento no duplica— pero estas tres no se hicieron y no se van a dar por hechas. **(a) Q13, el vencimiento a las 24 horas**: pide adelantar el reloj del sistema, asi que solo se puede en el emulador. Lo que queda sin comprobar es que el proveedor respete el `ttl: 86400s`, que es una promesa de Google y no codigo nuestro; el valor que mandamos si esta fijado por una prueba. **(b) Q16, la prueba de varios dias sin abrir la app**: es el caso que mata a los push —el fabricante duerme la app— y por definicion no se ve el primer dia. **Solo la cierra el telefono de Diego**, y depende de los dos gestos del ahorro de bateria del runbook. **(c) Q17 / T039**: preguntarle a Diego si, leyendo solo el aviso y sin desbloquear, sabe si le queda de paso. Si contesta "tengo que abrir igual", el renglon se corrige con lo que el diga — **no agregando un campo al formulario del cliente**. **Disparador**: (b) y (c), la primera semana de uso real; (a), la proxima vez que se toque el armado del mensaje. |
| 2026-09-01 | **High** | **El spec, el plan, el ADR y el quickstart de `018` dicen que el telefono de Diego es un Samsung con One UI, y el repo tiene medido que es un Redmi con HyperOS.** La medicion esta en [`processes/app-repartidor.md`](processes/app-repartidor.md), fechada el **2026-08-26** y hecha con `adb shell getprop`: *"un Redmi con Android 16 (API 36)"*, y hay una seccion entera del mismo documento sobre las trabas de instalacion **de Xiaomi**. Contra eso, `018` habla de Samsung en cinco lugares —spec (caso de borde y FR-011), plan (contexto tecnico y paso 8), quickstart (Q14, Q16 y el encabezado del nivel 3) y el ADR `push-as-the-only-alert`—. **Por que importa y no es un detalle de redaccion**: el gesto que hay que darle al telefono para que no duerma la app **se llama distinto y esta en otro lado** en cada capa de fabricante. Las instrucciones de Samsung —*Bateria → Apps que nunca duermen*— mandan a Diego a una pantalla **que su telefono no tiene**; en HyperOS son **dos** ajustes separados (*Ahorro de bateria → Sin restricciones* e *Inicio automatico*), y omitir el segundo deja la app sin recibir avisos con la app cerrada. O sea que seguir el documento al pie de la letra produce exactamente el sintoma que `018` existe para evitar, y **falla en silencio**: no hay error, simplemente no llega nada. **Ya corregido donde se ejecuta**: la seccion nueva de `app-repartidor.md` documenta los dos gestos de HyperOS, con los equivalentes de Samsung y de Android sin capa. **Lo que queda abierto** son los artefactos de `018`, que siguen afirmando el fabricante equivocado, y **cual de las dos afirmaciones es la verdadera** — puede haber cambiado de telefono despues del 2026-08-26, y en ese caso lo que hay que corregir es el documento y no el spec. **Disparador**: antes de correr Q14 y Q16 con el telefono de Diego en la mano (T038 de `018`), porque son justo las dos pruebas que dependen del fabricante. Preguntarselo cuesta un mensaje. |
| 2026-09-01 | Medium | **La app usa dos APIs de `firebase-messaging` marcadas como obsoletas, a proposito, y conviene que quede escrito por que.** Son `FirebaseMessaging.getToken()` y `FirebaseMessagingService.onNewToken()`, en `android/app/src/main/java/uy/flashurbano/repartidor/datos/Avisos.kt` y `AvisosService.kt`. El compilador las avisa desde la version **25.1.2** (BOM `34.18.0`). **Por que no se migro, medido y no supuesto**: el reemplazo es `register()` + `onRegistered()`, y desensamblando el artefacto (`javap -c` sobre el `classes.jar` del AAR) lo primero que hace `register()` es `if (!isV1RegistrationEnabled()) return Tasks.forException(IllegalStateException("API disabled. Please enable it by adding firebase_messaging_installation_id_enabled=true to your app's manifest"))`. O sea que **el reemplazo esta apagado por defecto**: migrar no es cambiar dos llamadas, es optar por un modelo de registro distinto con una bandera en el manifiesto, y ademas cambia de donde sale el token —`register()` no lo devuelve, llega por el callback del servicio—. Eso pide su propia comprobacion en un telefono de verdad, y hacerlo de paso mientras se construia `018` habria sido meter un riesgo nuevo adentro del feature que existe para que Diego se entere de los pedidos. **Como se paga**: prender la bandera, mover el guardado a `onRegistered`, y correr el nivel 3 del quickstart de `018` entero —instalacion limpia incluida, porque lo que cambia es justamente el registro—. **Disparador**: que Firebase anuncie la fecha de remocion, o el primer sintoma de token que no se renueva. Mientras tanto las dos llamadas llevan `@Suppress("DEPRECATION")` **con el argumento completo escrito al lado**, no un silenciador pelado. |
| 2026-08-31 | Medium | **`railway-despliegue.md` no dice como llegar a la consola de Postgres**, y dos documentos ya la dan por sabida: el procedimiento de cortar sesiones de `012` y las secciones que `017` agrego a `app-repartidor.md`. **Lo que costo averiguarlo**: la pestana **Console** del panel es un **bash dentro del contenedor**, no una consola SQL — pegarle SQL responde `SELECT: command not found`; hay que entrar con `psql -U postgres -d railway`. Y **las dos vias por CLI no funcionan en esta maquina**: `railway connect postgis` exige un `psql` local que no esta en el PATH, y `railway ssh -s postgis` pide una clave SSH que no existe. El servicio solo expone `DATABASE_PRIVATE_URL`, que no resuelve desde afuera. **Disparador**: la proxima vez que haya que mirar la base a mano, o antes si se agrega una clave SSH. |
| 2026-08-30 | Low | **La cedula de quien recibe se guarda tal como Diego la escribe**, sin validar digito verificador ni normalizar puntos y guiones (`016`, research D5). **Por que se decidio asi**: es un respaldo que se anota parado en una puerta, no una clave. Rechazar `1234567-8` porque se esperaba `1.234.567-8` le traba a Diego una entrega **que ya ocurrio**, con la persona delante, por un problema de tipeo. **Lo que cuesta**: no se va a poder buscar por cedula sin normalizar primero, y como los formatos van a estar mezclados, esa normalizacion sera sobre datos y no sobre un formato acordado. **Disparador**: el dia que haga falta buscar o cruzar por cedula. Ahi se normaliza con los datos a la vista, que es mejor que adivinar el formato hoy. |
| 2026-08-30 | Medium | **La app no tiene pruebas instrumentadas, y `015` mostro exactamente lo que eso cuesta.** Las dos pruebas que agrego —contraste de la paleta y piso de 48 dp— corren en la JVM y cubren **los valores, no el dibujo**: las dos lo dicen adentro. En una sola jornada el emulador encontro **tres defectos que ninguna de ellas podia ver**: la barra de destinos en lila (`surfaceContainer` sin declarar), la accion del Snackbar en lila (`inversePrimary` sin declarar), y un spinner en las acciones apagadas sin red —que decia "espera que esta yendo" cuando no iba nada—. **Que se pagaria con esto**: pruebas de interfaz de Compose (`androidx.compose.ui.test`), que necesitan un emulador en CI y son infraestructura nueva. `015` decidio no traerlas (research D5) porque son cuatro pantallas y el costo no se justifica todavia. **Disparador**: cuando la app gane una segunda pantalla con logica propia, o cuando un defecto visual llegue al telefono de Diego en vez de morir en el emulador. |
| 2026-08-26 | **Resolved por `015`** | **RESUELTA el 2026-08-30, con una salvedad: el contraste al sol sigue sin evaluar y Diego todavia no la vio.** Lo que esta fila pedia —alcance del pulgar, tamano de toque, paleta de marca en vez del morado, devolucion visible— esta entregado, y el alcance del pulgar lo probo Mateo con una mano en un telefono real. Texto original: **La app de `012` es funcional pero el diseño quedó al piso de Material 3, y no alcanza para una herramienta de calle.** Dicho por el cliente el mismo dia que la instalo y la uso contra produccion: la ve **"medio fea"**, la quiere **mas moderna y mas colorida**, con **mas devolucion de lo que esta pasando** y **mas accesibilidad** — puntualmente **donde apoyar el dedo** y la facilidad de uso. **No es cosmetico y no es una queja suelta**: el plan de `012` decia desde el principio que la app "es una herramienta de calle, no un panel", que se usa **con una mano mientras la otra tiene un paquete**, y que **cada correccion posterior es un archivo nuevo instalado a mano en el telefono de otra persona** — o sea que el costo de iterar aca es mas alto que en la web. Hoy la app usa el tema morado por defecto de Material 3, **sin nada de la marca**. **Que no se hizo y por que**: `012` era la rebanada base y su alcance era que el dato llegara y el estado se moviera; el diseño no tenia requisito propio. **Lo que hay que mirar cuando se abra**: alcance del pulgar (los botones de accion estan al final de tarjetas largas, o sea arriba en la pantalla despues de desplazar), tamano de toque, contraste al sol, la paleta de marca en vez del morado, y devolucion visible en cada accion. **T2 y T3 del quickstart siguen sin evaluar** y son justamente eso. Feature propio, no parche. |
| 2026-08-26 | High | **`GET /admin/pedidos` devuelve TODOS los pedidos de todo el mundo, sin paginar, y la app los baja enteros en cada refresco.** Medido el 2026-08-26 contra el servicio local: **~710 bytes por pedido**. O sea 500 pedidos = 0,34 MB por refresco, 2.000 = 1,35 MB, 5.000 = 3,39 MB. Diego toca *Actualizar* varias veces por dia y lo hace **con datos moviles, en la calle**: a 2.000 pedidos, veinte refrescos son 27 MB en un dia. **El umbral no es el tamano, es el ritmo de crecimiento**: hoy el negocio hace del orden de decenas de pedidos por semana, asi que 2.000 esta a mas de un ano — pero nada avisa cuando se cruza, porque la app sigue andando cada vez un poco mas lenta. **Como se paga**: que `GET /admin/pedidos` acepte un filtro por estado (la app solo trabaja pendientes y tomados; los entregados son la lista que crece) o un limite con paginado. Lo primero es mas barato y resuelve el 90%. **Disparador para hacerlo**: cuando la tabla `pedidos` pase de 1.000 filas, o cuando el refresco tarde mas de dos segundos en el telefono. |
| 2026-08-26 | Medium | **La base de desarrollo local puede diverger del esquema del repo, y ya divergio en la columna exacta que tumbo produccion.** Encontrado el 2026-08-26 armando el caso E2 de `012`: `flash-pg-dev` tenia `pedidos.entrega_punto NOT NULL`, o sea la version de `0004` **anterior** a la correccion del 2026-08-23, mientras produccion y la base de pruebas la tienen nullable. La causa es estructural y no un descuido: **una migracion que ya figura en `migraciones_aplicadas` no vuelve a correr**, asi que editar el archivo despues de aplicarlo deja a esa base congelada en la version vieja para siempre. Se alineo a mano con `ALTER TABLE pedidos ALTER COLUMN entrega_punto DROP NOT NULL`. **Que cuesta**: lo que se prueba en local deja de decir lo que pasaria en produccion, en cualquier direccion. **Como se paga**, en orden de esfuerzo: (a) dejar escrito en `dev-setup.md` que ante cualquier rareza de esquema la respuesta es recrear la base de dev, que es descartable; (b) que `dev.sh` compare el esquema real contra el que producen las migraciones desde cero y avise; (c) no volver a editar una migracion aplicada nunca, y corregir siempre con una migracion nueva — que es la regla que `0004` rompio y que su propio comentario dice para `0003`. |
| 2026-08-26 | Medium | **No hay revocacion remota de sesion: cortarle el acceso a un telefono perdido se hace a mano en la base.** El procedimiento esta escrito en [`processes/app-repartidor.md`](processes/app-repartidor.md) y funciona, pero exige que alguien con acceso a la consola de Railway lo ejecute. Ademas **corta TODAS las sesiones del usuario, no la del telefono perdido**: lo unico guardado es el hash del token, y el token esta en el telefono, asi que desde la consola no hay forma de distinguir una fila de otra. **Mientras haya un solo repartidor alcanza** — Diego vuelve a ingresar en el telefono que tenga. **Disparador: el dia que haya un segundo repartidor esto se vuelve obligatorio como funcion del producto**, porque cortar la sesion de uno dejaria afuera al otro. Ahi hace falta guardar algo que identifique el dispositivo y una pantalla para cerrarlo. |
| 2026-08-26 | Low | **Que pedidos decide llevar Diego cada dia no queda registrado en ningun lado.** La app le muestra pendientes y tomados y el elige con la cabeza; lo unico que se escribe es el estado y su historial (`pedidos_estados`), o sea el resultado, no la decision. **Que cuesta**: no se puede reconstruir por que un pedido quedo para el dia siguiente, ni medir si la eleccion fue buena. **Por que se dejo afuera a proposito**: registrar la intencion antes de que ocurra exige una pantalla de armado de recorrido que nadie pidio, y el feature de ruta —que es donde eso tendria sentido— **sigue esperando que el cliente defina que es una "ruta economica"**. |
| 2026-08-26 | Medium | **No hay ruta ni ordenamiento por proximidad, y PostGIS sigue puesto para eso sin usarse.** La app **no decide un orden de visita ni sugiere un recorrido**: lo prohibe explicitamente el contrato de `012` seccion 6, y esta bien que lo prohiba, porque sugerir un orden sin saber que optimiza es peor que no sugerir nada. La extension espacial esta desde el dia uno (ADR `backend-persistence-stack`) y los puntos se guardan **justamente** para esto. **Que falta para poder hacerlo**: que el cliente defina que quiere optimizar — kilometros, tiempo, o cantidad de entregas por vuelta—, que es la pregunta abierta que [`preguntas-cliente.md`](preguntas-cliente.md) llama "ruta economica". **Sin esa respuesta no se puede empezar**, y con ella es un feature propio. |
| 2026-08-22 | Medium | **El `verify:` del harness puede dar verde sin haber corrido las pruebas que importan, y no hay nada que lo detecte.** Las pruebas de Go contra Postgres se saltean solas sin `TEST_DATABASE_URL`; `internal/pedidos` tiene 23 asi. En `011` el `go test ./...` dio `ok` **sobre el paquete que el feature estaba cambiando**, y al levantar la base de pruebas aparecieron cuatro fallas reales, incluida una que rompia al leer cualquier pedido sin punto de retiro — o sea el caso que el feature crea a proposito. **Es un sensor faltante, no un descuido de quien corre el comando**: el harness pregunta "guia faltante o sensor faltante" y esta es claramente la segunda. Ya mordio dos veces (`010` lo anoto, `011` lo vivio). **Como se paga**, en orden de esfuerzo: (a) que el `verify:` de un plan que toca `backend/` exija la variable; (b) que las pruebas fallen —en vez de saltearse— cuando detectan que hay un Postgres de pruebas disponible y no se les paso la URL; (c) un chequeo en `scripts/harness/` que rechace un verde con skips por encima de un umbral. Mientras tanto vive como guia en `docs/processes/harness.md`, que es lo que el harness llama tapar un sensor con una guia. |
| 2026-08-22 | Medium | **Con el servicio caido, `/perfil` le dice "Ingresa para ver y editar tus datos" a alguien que SI tiene sesion.** Lo encontro H2 del quickstart de `011` —un paso heredado de `010` que nunca se habia corrido— y **no lo introdujo `011`**: viene de `006`/`007`. **La credencial no se pierde**, y eso esta bien resuelto y comentado en `web/components/sesion/proveedor-sesion.tsx:110`: solo un 401 la borra; un fallo de red no, porque reingresar tocaria el mismo servicio que acaba de fallar. Lo que falta es la PANTALLA: si `GET /yo` no contesta, `usuario` queda en `null` y `/perfil` dibuja la rama de "sin sesion". **Hay tres estados y solo se dibujan dos**: identificado, no identificado, y **no sabemos porque el servicio no responde**. **Que cuesta**: quien recarga durante un corte cree que se quedo afuera. Recuperarse es recargar cuando el servicio vuelve —no hay dato perdido— pero la pantalla miente mientras tanto, y encima **tapa el estado de error del historial de `010`**, que no se llega a mostrar nunca. **Como se paga**: distinguir en el proveedor "sin credencial" de "con credencial y sin respuesta", y que `/perfil` muestre en el segundo caso un aviso de servicio no disponible con reintento, en vez de la invitacion a ingresar. **No se arreglo en `011`** porque el archivo esta fuera de su `covers:` y porque que diga la pantalla es una decision de producto, no una obvia. |
| 2026-08-22 | Medium | **Un pedido cuyo retiro no resuelve a ningun punto se guarda sin coordenadas, en silencio, y nadie se entera hasta que Diego avisa que no le anda el mapa.** Son **dos** caminos hacia la misma fila sin coordenadas: el texto no resuelve a ningun cruce (FR-015), o la calle es **homonima** y hay varios candidatos, en cuyo caso tampoco se pregunta (FR-014, corregido el 2026-08-22). Es la decision de FR-015 de `011`, tomada a la vista y por un motivo con fecha de vencimiento: **todavia no hay nadie en produccion**, y trancar el primer pedido real por un hueco del indice de calles es peor que guardar una direccion escrita sin punto. **Lo que cuesta**: (a) FR-011 —el retiro tiene que estar en el area— pasa a ser de mejor esfuerzo, porque el area solo se comprueba donde el texto resuelve; (b) `pedidos.retiro_punto` deja de ser `NOT NULL`, asi que **la app Android tiene que tolerar un retiro sin coordenadas** y hay que decirselo a quien la construya; (c) el hueco se detecta por el canal humano, no por el sistema. **Medido el 2026-08-22, y el numero NO es una tasa**: 2 de 2 pedidos quedaron con `retiro_punto IS NULL`, pero los dos se crearon a proposito para disparar los dos caminos de falla (`FU-0011` con una calle inventada, `FU-0012` con el par homonimo Valencia/Zaragoza). Sobre un conjunto armado para fallar, el 100% no dice nada de la frecuencia real. **Lo que si quedo demostrado es que los dos mecanismos funcionan.** La medicion que importa necesita transito de verdad. **El disparador para pagarla**: el primer pedido real en produccion, o el primer reclamo de Diego por una direccion que no pudo ubicar. **Como se paga**: contar cuantos pedidos quedan sin punto de retiro —`WHERE retiro_punto IS NULL` es la consulta entera— y, si son mas que unos pocos, o se tapan los huecos del indice de calles o se le pide la esquina a quien envia cuando el texto no resuelve. Sin esa medicion no se sabe si el hueco es marginal o cotidiano, y **hoy no hay ni un dato**. |
| 2026-08-22 | **High** | **Una direccion sobre una avenida limite paga la zona cara o la barata segun donde cayo el pin, con precision de centimetros.** Medido sobre un pedido real: el punto de `FU-0005` (Avenida Carlos Maria Ramirez 476 esq. Julian Laguna) esta a **0,4 m** del borde de la zona 1 y por eso pago $200 en vez de $150; repetirlo hoy recoloca el punto 46 m y sale $150. **La regla no falta** — `resolverZona()` ya devuelve la zona mas barata cuando dos poligonos reclaman el punto (FR-020 de `004`, contestada por el cliente el 2026-08-06) — lo que falta es superficie donde esa regla actue: los poligonos siguen el eje de la avenida y hoy las zonas 1 y 2 se pisan en **4.310 m²** en total, una astilla. **Como NO se paga**: (a) ensanchando los poligonos, porque el mapa del sitio dibuja `zona.anillo` y las franjas superpuestas se ven como un error de trazado; (b) resolviendo la zona en el backend, porque `backend/internal/pedidos/pedido.go` no resuelve zonas a proposito y el precio se calcula en el navegador para poder cotizar con el servicio caido (Principio V, FR-022, con prueba). **Como se paga**: separando los dos trabajos que hoy hace el anillo —pintar y cobrar—. El mapa sigue dibujando el anillo del KML sin tocar, y `resolverZona()` gana una regla de distancia: a menos de N metros del borde de una zona mas barata, cobra la barata. Propuesta de N: **15 m**, muy por debajo de media cuadra (~80-100 m) para que no se trague direcciones que estan genuinamente una calle adentro. **El cliente la despriorizo el 2026-08-22**: Diego dijo que el caso lo maneja el a mano y que esto queda como mejora, no como arreglo. Se deja anotada y **no se abre feature**. Lo que la volveria urgente es que empiece a pasar seguido sin que el se entere — o sea, cuando haya volumen real de pedidos sobre las avenidas limite. Si se retoma: la regla y el numero los tiene que confirmar el, igual que confirmo la regla original el 2026-08-06, y el cambio pide ADR por separar lo que se pinta de lo que cobra. |
| 2026-08-22 | **High** | **`010` agranda el agujero de verificacion de la fila del 2026-08-14 de una correccion puntual a una seccion entera del producto, y esta vez fue una decision tomada a la vista, no un descuido.** Se pregunto explicitamente el 2026-08-22 si montar un entorno con DOM, probar la logica en `lib/` y la pantalla a mano, o todo a mano; se eligio **todo a mano, documentado** (spec de `010`, sesion de clarificacion). Lo que queda **sin ninguna prueba automatica** son tres pantallas: `web/components/pedido/historial.tsx` (la lista, sus tres estados vacios, el reintento), `web/components/pedido/tarjeta-pedido.tsx` (el detalle desplegable y su accesibilidad de teclado), y **el camino de precarga por `?repetir=` de `web/components/pedido/crear-pedido.tsx`** — que es el mas caro de los tres, porque es exactamente donde vive la forma del defecto del 2026-08-14: si esa precarga desmonta `PedidoForm`, se pierde lo tipeado y **nada se pone en rojo**. Lo que si quedo cubierto es `web/lib/repetir.ts` (24 casos, con la guarda de imports y su control positivo) y lo reemplaza en lo demas `specs/010-mis-pedidos/quickstart.md`, que exige producir los casos malos a proposito. **Tercer requisito distinto pidiendo la misma herramienta**: FR-001 (2026-08-10), FR-007 (2026-08-14) y ahora US1/US2 de `010`. |
| 2026-08-22 | Low | **El historial no tiene paginado y trae todos los pedidos de la persona en una sola respuesta.** `GET /pedidos` no acepta limite ni cursor, y `historial.tsx` muestra los 5 mas recientes con un "Ver todos" **sobre lo que ya llego**: no es paginado, es recorte de pantalla. **El umbral, en numeros**: cada pedido pesa ~700 bytes de JSON, asi que 100 pedidos son ~70 KB y 500 son ~350 KB sin comprimir. Se vuelve incomodo en un telefono con datos moviles **arriba de ~300 pedidos por persona**, y molesto de verdad arriba de ~1000. Con el volumen actual —los codigos van por `FU-00xx`— falta mucho para eso, y por eso `010` no lo construyo (Principio III). Se paga agregando `?limite=` y `?desde=` al endpoint y scroll incremental en la pantalla; el indice que hace falta (`pedidos_usuario_idx`, por `usuario_id, creado_en DESC`) **ya existe** desde `0003`. |
| 2026-08-14 | Medium | **La segunda prueba manual seguida encontró que identificarse a mitad de formulario borraba todo lo tipeado, y ninguna prueba automática podía verlo.** FR-007 prohíbe que identificarse reescriba lo que la persona acaba de escribir, y `usePrecarga` estaba construido para cumplirlo: el efecto corría una sola vez. **El efecto no era el problema.** Al entrar por el diálogo, el usuario pasaba de `null` a tener sesión con dirección guardada, y la rama derivada devolvía `listaLaPrecarga: false` mientras rehidrataba — eso **desmonta `PedidoForm`**, que al volver se monta de cero con el `inicial` del perfil. No era "la precarga corre dos veces" sino "el formulario se monta dos veces", y por eso el razonamiento que protegía el requisito miraba el lugar equivocado. **Arreglado el mismo día** (la decisión se toma una vez adentro del efecto y ya no se deriva de la sesión), así que lo que queda abierto no es el defecto sino **el agujero de verificación**: `web/` corre en entorno `node` sin DOM y nada de este repo renderiza React, así que la corrección **sólo está sostenida por una prueba manual**, y una regresión no pondría nada en rojo. Es la segunda vez que este mismo agujero deja pasar algo — ver la fila `Medium` del 2026-08-10 sobre FR-001, que pedía lo mismo por otro requisito. Las dos se cierran con el mismo trabajo: mover `web/` a un entorno con DOM (`jsdom` o `vitest-browser`) y montar `CrearPedido` con la sesión simulada, tipear, identificarse, y afirmar que el formulario conservó lo tipeado. **Dos requisitos distintos pidiendo la misma herramienta es la señal de que ya no es opcional.** |
| 2026-08-14 | **High** | **~~Un cliente que confirma un pedido recibe un código y silencio~~ — el circuito lo cierra `018`, y queda `High` hasta verlo sonar en un teléfono.** **Al 2026-09-01**: el servicio le manda un aviso push a Diego al crear un pedido, la app lo recibe y lo abre en ese pedido, y todo eso esta cubierto por pruebas —265 del backend en verde con base, mas las de JVM de la app—. **Lo que las pruebas NO cubren es lo unico que cierra esta fila**: que el aviso **llegue al telefono de Diego**, que no es observable desde un `verify:` (research D7 de `018`). Eso se mide en el nivel 3 del quickstart, y hasta que eso pase esta fila **no baja de `High`**: un aviso que se manda y no llega deja el circuito exactamente igual de abierto que no mandarlo, con el agravante de que ahora parece resuelto. La afirmacion de que **la app Android no existe** si esta vencida y era lo que esta fila necesitaba corregir (T034 de `018`): existe desde `012`, se instala por link desde `017`, y desde `018` avisa. **Sigue siendo un bloqueante para promocionar el sitio** hasta Q14/Q16. Texto original abajo, sin tocar:** Un cliente que confirma un pedido recibe un código y silencio: nada le avisa a Diego, y desde hoy la pantalla tampoco le ofrece por dónde preguntar.** El pedido se guarda de verdad —eso lo resolvió `007`— pero el aviso a Diego vive en la app Android, que no existe, así que hoy los pedidos se leen corriendo `curl` contra `GET /admin/pedidos`. Hasta el 2026-08-14 la pantalla de confirmación compensaba ese hueco enlazando a `/contacto` con el texto *"y escribinos por WhatsApp con ese código para coordinar el retiro"*, que era el único camino por el que un pedido llegaba a un humano. **El dueño del proyecto lo sacó a propósito**, con un motivo coherente con el estado final: la coordinación la arranca Diego desde la app, que va a tener el contacto del cliente, y empujar al cliente a WhatsApp repone justo el canal manual que el producto existe para reemplazar (Principio II). FR-029 quedó enmendado con ese motivo. **Lo que la decisión deja abierto y esta fila existe para no perder**: entre hoy y la app Android, el circuito no se cierra solo — el pedido queda en la base y nadie mira. Se acepta porque el sitio **todavía no se promociona** y `/contacto` sigue en la navegación, así que el cliente que insista tiene por dónde. **Es un bloqueante para promocionar el sitio**, no una deuda cosmética: el día que entre tráfico real, un pedido sin aviso es un cliente esperando un retiro que nadie agendó — la misma falla que la fila `High` del 2026-08-12, con la promesa explícita quitada pero el hueco intacto. Salidas, de menor a mayor: que Diego mire `GET /admin/pedidos` con una rutina fija mientras dure el interín (proceso, no código); un aviso por mail a Diego al crear un pedido, que reusa el paquete `correo` que `006` ya construyó y son pocas líneas; o la app Android, que es el plan. |
| 2026-08-12 | Medium | **El servicio guarda el precio y la zona que declara el navegador, sin verificarlos.** Lo obliga a registrar FR-021a de `007`, y es una decisión tomada **con el riesgo a la vista** el 2026-08-12, no un descuido: quien arme la petición a mano puede declarar cualquier monto —`precio: 1` se guarda— **y también un punto fuera de cobertura** (FR-020a), porque el servicio no resuelve zonas. Lo único que comprueba es la forma: `precio > 0` y `zonaId` entre 1 y 5. **Dos cosas acotan el daño y conviene no perderlas de vista.** La primera es que el **punto queda guardado**: el precio es recalculable en cualquier momento, así que la verificación se pospone y no se pierde — por eso `retiro_punto` es `NOT NULL` y por eso esa columna no es un detalle del esquema sino la mitad de esta decisión. La segunda es que **Diego mira el pedido antes de aceptarlo**: hay un humano en el circuito, hoy. Se descartó resolver la zona del lado del servidor porque duplicaría la geometría que decide la plata, y dos fuentes que se desincronizan producen el peor defecto posible acá: el cliente ve un precio y se le cobra otro. **La salida evaluada y no construida**, para que la próxima discusión no arranque de cero: el servicio **no necesita la geometría para saber que la Zona 3 cuesta tanto** — cinco zonas, cinco precios, una tabla sin un solo polígono. Con eso, `precio: 1` se rechaza y el riesgo baja de *"puede declarar cualquier monto"* a *"puede declarar una zona que no es la suya"*, que es mucho más chico. No se hizo porque pondría los precios en un segundo lugar: hoy viven en `web/lib/zonas.ts`, que es **archivo generado** desde `web/design-source/build-zonas.js`, y un precio cambiado en un lado y no en el otro no produce un precio equivocado sino **pedidos rechazados** — peor, porque el cliente no puede comprar y nadie sabe por qué. La forma correcta es generar la tabla del servicio desde la misma fuente, y eso es trabajo en `web/design-source/`, fuera del alcance de `007`. |
| 2026-08-12 | Low | **El helper que vacía la base de pruebas tiene la lista de tablas escrita a mano, así que se pudre con cada migración que agregue una.** `vaciar()` en `backend/internal/db/migrate_test.go` hace `DROP TABLE IF EXISTS` sobre cinco nombres fijos, heredados de `0001`. `007` agregó `pedidos` y **el olvido no se lee como lo que es**: la tabla sobrevive al vaciado, la migración siguiente choca contra sí misma, y el error apunta al objeto equivocado — en este caso `relation "pedidos_codigo_seq" already exists`, o sea a la secuencia y no a la tabla que la creó, mandando a quien depure a mirar la secuencia. **El `CASCADE` no protege**, y esa es la parte contraintuitiva: se lleva las claves foráneas que *apuntan* a las tablas listadas, no las tablas que las contienen, así que `pedidos` sobrevive justamente por referenciar a `usuarios`. Se descubrió corriendo las pruebas contra Postgres de verdad, no leyendo. Arreglo hecho en `007`: sumar `pedidos` a la lista, que es lo mínimo para que el `verify:` pueda estar verde. Arreglo de fondo, no hecho porque `AGENTS.md` prohíbe la limpieza oportunista fuera de los pasos del plan: **borrar dinámicamente** —consultar `information_schema.tables` del esquema `public` y dropear todo— con lo que la próxima migración que agregue una tabla no requiere acordarse de nada. Son unas diez líneas y elimina la clase entera. **AMPLIACIÓN del mismo día: no era un helper, eran cuatro, en tres paquetes.** Al sumar la FK `pedidos → usuarios` con `ON DELETE RESTRICT`, todo helper que hiciera `DELETE FROM usuarios` pasó a fallar si quedaba algún pedido — y como `auth` corre **antes** que `pedidos` en el orden alfabético de `go test ./...`, el síntoma es un rojo en un paquete que no tiene nada que ver, con un error de clave foránea que no lo menciona. Hubo que tocar `db/migrate_test.go`, `auth/sesion_test.go`, `usuarios/usuario_test.go` y `auth/handlers_codigo_test.go`. **El último casi se escapa**: arma la consulta concatenando (`"DELETE FROM " + tabla`) sobre una lista de nombres, así que un `grep "DELETE FROM usuarios"` daba dos resultados cuando había tres. Sólo apareció enumerando a mano los cinco helpers de setup del paquete `auth`. Lección para la próxima migración que agregue una tabla con FK: **el grep literal no alcanza**, y el modo de falla no se lee como lo que es. Refuerza el arreglo de fondo: con borrado dinámico, ninguno de los cuatro habría necesitado tocarse. |
| 2026-08-12 | ~~**High**~~ | **~~El sitio le dice al cliente "¡Pedido cargado! Nos pondremos en contacto para confirmar el retiro"~~ — RESUELTA por `007` el 2026-08-15.** Las dos mitades se cerraron: el pedido **se guarda de verdad** en Postgres y vuelve con su código `FU-####` —comprobado creando pedidos desde el sitio y desde un teléfono—, y **la frase ya no existe**: la confirmación dice únicamente *"Anotá tu código FU-####"*. **Lo que NO resolvió `007`, y por eso nació la fila `High` del 2026-08-14**: que Diego se entere. El aviso vive en la app Android, que no existe, así que hoy los pedidos se leen con `GET /admin/pedidos`. La diferencia con el estado anterior es real y vale nombrarla —antes el pedido **no existía en ningún lado** y encima se prometía un contacto; ahora existe, es recuperable íntegro, y no se promete nada falso— pero el circuito sigue sin cerrarse solo. Texto original abajo, para no perder qué decía:** El sitio le dice al cliente "¡Pedido cargado! Nos pondremos en contacto para confirmar el retiro" — y no se guarda nada, no se le avisa a nadie, y nadie se va a poner en contacto.** `web/components/pedido-form.tsx:609` renderiza ese texto al confirmar. No hay pedido en ninguna base, no sale ningún aviso, y el estado vive en `useState` hasta que la persona cierra la pestaña. **Es distinto de la fila de abajo y peor**: aquella es una contradicción entre dos documentos del repo —grave, pero interna—; ésta es una promesa que el producto le hace a una persona real y no puede cumplir. El sitio es indexable desde `004` y está en un dominio propio desde `009`, así que el camino existe: alguien llega desde un buscador, carga su envío, lee que lo van a contactar, y espera un retiro que nadie agendó. **No estaba registrada en ningún lado antes del spec de `007`**, ni siquiera como nota — se descubrió leyendo el componente para escribirlo, no en una revisión. Que haya sobrevivido a cuatro features es el dato interesante: la pantalla *parece* terminada, y ni el spec de `001` ni los de `004`/`005`, que tocaron el formulario, miraron qué prometía el final del flujo. Se cierra con `007` **sólo si `007` resuelve que Diego se entere**: guardar el pedido en Postgres corrige "no se guarda nada" y deja intacto "nadie se va a poner en contacto". Es la clarificación 1 de ese spec. Si se difiere el aviso, entonces el arreglo obligatorio de `007` es el copy (FR-029): la pantalla no puede seguir prometiendo un contacto que no existe. Mitigación mientras tanto: ninguna aplicada. |
| 2026-08-11 | ~~**High**~~ | **~~El formulario de pedido contradice la constitución desde la v3.0.0~~ — RESUELTA por `007` el 2026-08-15.** Las dos obligaciones que la enmienda nombraba están cumplidas y **verificadas a mano en un teléfono**, no sólo en verde: (a) **la puerta está puesta** — confirmar exige sesión, se abre un diálogo sobre la misma pantalla sin cambiar la URL, y el envío se reanuda solo sin que la persona reescriba nada ni toque confirmar dos veces; (b) **el copy de `/pedido` ya no invita a pedir sin cuenta**. El código dejó de contradecir al documento de máxima autoridad del repo. La otra mitad de la constitución —que cotizar siga abierto con el servicio apagado— sigue protegida por la guarda de `cotizar-abierto.test.ts`, que en `007` ganó un **control positivo** para que borrar el envío entero no la deje en verde. Texto original abajo:** El formulario de pedido contradice la constitución desde la v3.0.0, y va a seguir haciéndolo hasta que `007` ponga la puerta.** La enmienda del 2026-08-11 dice *"No package is created without an identified customer"*; `web/components/pedido-form.tsx` sigue aceptando y confirmando un pedido de un visitante anónimo, y el copy de `/pedido` todavía invita a hacerlo: *"Podés cargarlo como invitado, sin necesidad de crear una cuenta"*. **Es deliberado y tiene dueño** —`006` excluyó el formulario de su `covers:` a propósito (FR-007b), porque la puerta y el pedido que se guarda de verdad tienen que salir juntos: exigir registro para llegar a una pantalla que no guarda nada no le compra nada al cliente— pero mientras tanto **el documento de máxima autoridad del repo dice una cosa y el código hace otra**, y eso se anota en vez de dejarlo implícito: quien lea la constitución sin esta fila va a encontrar una violación y no va a saber si es conocida. Se cierra con `007`, que debe (a) exigir sesión antes de confirmar y (b) corregir ese copy en el mismo cambio. Riesgo mientras tanto: **acotado pero no nulo** — hoy el pedido no le llega a nadie, así que un pedido anónimo no genera una obligación que Diego no pueda cumplir; el día que `007` lo persista sin la puerta, sí. |
| 2026-08-11 | Medium | **No hay forma de que un cliente borre su cuenta, y ahora la cuenta guarda su dirección.** `006` lo declaró como deuda en las Assumptions de su spec y lo deja abierto al cerrar: el servicio crea usuarios —dos caminos lo hacen— y no ofrece ninguno para darse de baja. Lo que se guarda es nombre, teléfono, dirección de retiro con el punto exacto de la puerta, y el rastro de ingresos con la dirección de mail. La purga por antigüedad recorta el rastro a noventa días, pero **la fila del usuario no vence nunca**. Se agrupa con la fila `Low` del mismo día sobre no poder borrar la dirección: las dos son "el cliente quiere que dejemos de tener un dato suyo", y conviene resolverlas juntas en vez de agregar dos endpoints distintos. Arreglo: un `DELETE /yo` que borre la fila y sus sesiones por cascada, y decida explícitamente qué pasa con el rastro —borrarlo pierde la capacidad de reconstruir un reclamo, conservarlo con la dirección de mail contradice la baja—. Es una decisión de producto antes que de código. **Dirección decidida el 2026-08-12 por el dueño del proyecto**: la baja la maneja **Diego desde el dashboard de la app Android**, y se implementa como **borrado lógico** —una columna de activo/inactivo, y las consultas traen sólo los activos—. Eso resuelve el problema operativo y **hay que decir cuál no resuelve**: un usuario inactivo **sigue teniendo guardados** nombre, teléfono, dirección de retiro y el punto exacto de la puerta. Si algún día la baja se pide por privacidad —"quiero que dejen de tener mis datos"— una bandera no la satisface, y las dos cosas se van a seguir llamando igual mientras nadie las separe. Son dos problemas distintos con el mismo nombre: **desactivar** (operativo, Diego, ya decidido) y **borrar** (privacidad, el cliente, sin decidir). El día que haga falta el segundo, la fila del usuario tiene que poder vaciarse conservando lo que el negocio necesite. **Compatible con `007`**: el pedido copia los datos en vez de referenciarlos, así que desactivar un usuario no toca ningún pedido histórico, y el `ON DELETE RESTRICT` de `pedidos.usuario_id` no estorba —con borrado lógico no hay borrado de fila que pueda chocar contra él, y queda como red por si alguna vez lo hay—. |
| 2026-08-11 | Medium | **El punto guardado en el perfil suma un consumidor al índice de calles, que no tiene verificación automática.** La fila `High` del 2026-08-04 dice que si una regeneración de `calles-mvd.json` desplaza las esquinas, el precio sale mal y ningún test lo detecta. Hasta `006` el único consumidor era el formulario de pedido, donde el punto se resuelve y se cobra en el mismo momento. Ahora hay un segundo: **el perfil guarda un punto que se leerá meses después**, y `007` lo va a precargar para cobrar sobre él. Eso cambia la forma del riesgo, no sólo su tamaño: un punto guardado en agosto y regenerado el índice en octubre puede quedar en otra cuadra —o en otra zona, o sea otro precio— sin que nadie toque nada. La rehidratación del perfil ya lo mitiga en parte —usa el punto guardado para desempatar entre cruces y, si ninguna cuadra lo contiene, cae al primero— pero **mitigar no es detectar**. Refuerza la prioridad del fixture de esquinas confirmadas a mano que pedía SC-002 de `003`. |
| 2026-08-11 | **High** | **Un rebote duro suprime la dirección en Resend, y el sitio va a seguir diciendo "te mandamos un código" para siempre.** Verificado el 2026-08-11 con un envío real: ante un `Recipient not found`, Resend marcó el mensaje `Bounced` y acto seguido `Suppressed` — la dirección entra en una lista y **no recibe más envíos, aunque se los pidas**. En el camino de ingreso por mail eso es una puerta que se cierra en silencio: si a un cliente le rebota una vez —casilla llena, una caída de su proveedor mal reportada, un rechazo temporal informado como permanente— queda suprimido, y desde ahí `POST /auth/codigo` responde lo mismo de siempre (FR-014 exige que no revele nada) mientras el mail nunca sale. **El síntoma lo sufre el cliente y la causa vive en un panel que nadie mira.** No lo cubre ninguna prueba: el doble de `correo/falso.go` no modela supresión. Arreglos, de menor a mayor: registrar en el rastro cuando el proveedor rechaza el envío, para que el fallo sea al menos reconstruible; consultar la API de supresiones de Resend antes de emitir y, si la dirección está suprimida, dar un mensaje distinto —con el cuidado de no violar FR-014, que prohíbe revelar si la dirección existe como usuario, no si es entregable—; o enganchar el webhook de rebotes de Resend para enterarse en el momento. **Hoy la Historia 3 no tiene ninguna de las tres**, y el camino por mail es el único que no depende de Google. **Ocurrió de verdad el 2026-08-11, y no por un rebote sino por una clave inválida**: en local, con una `CORREO_API_KEY` vieja, Resend devolvió `401 API key is invalid`; el código **se emitió y quedó vivo en la base**, el sitio contestó "te mandamos un código" —como FR-014 obliga— y el mail nunca salió. La única forma de enterarse fue leer el log del servicio. Eso amplía el alcance de esta fila: **no es sólo la supresión por rebote, es cualquier fallo del proveedor**, y una clave mal cargada es bastante más probable. Lo único que salvó el diagnóstico fue que `resend.go` registra el cuerpo de la respuesta en vez de tragarlo; sin eso, un `401` se veía igual que un servicio caído. |
| 2026-08-11 | Low | **Nada registra que un código de acceso salió bien — sólo se anota cuando falla.** `POST /auth/codigo` deja rastro de `limite_excedido` y escribe en el log cuando el proveedor rechaza, pero **en el camino feliz no escribe nada**. La consecuencia se vio el mismo día que se construyó: al intentar confirmar desde los logs de producción si un código había salido, **no había nada que mirar**, y la ausencia de errores era igual de compatible con "anduvo" que con "nadie lo intentó". Deja el diagnóstico dependiendo de que alguien haya estado mirando en el momento. Es la contracara de la fila `High` sobre fallos del proveedor: allí no nos enteramos de los errores, acá no tenemos constancia de los aciertos. Arreglo: una anotación en el rastro al emitir —con un resultado propio, porque `exito` hoy significa "entró" y no "se le mandó un código"—, lo que además haría reconstruible el reclamo *"pedí el código y nunca me llegó"*. Son pocas líneas y toca el `CHECK` de `resultado` en el esquema, así que necesita migración. |
| 2026-08-11 | Low | **~~SC-004 no se cumple en Outlook/Hotmail~~ — aceptado como decisión de negocio el 2026-08-11.** Medido con el correo real del código desde producción: **Gmail lo entrega a Recibidos** en menos de un minuto; **Hotmail lo manda a Correo no deseado**. **No es un defecto de configuración** y quedó demostrado: si la firma fallara, Gmail no lo pondría en Recibidos con un dominio de horas de vida. Lo que falta es reputación de dominio, que se construye mandando, y las IP son de Resend/SES así que no se pueden registrar en los programas de Microsoft. **El dueño del proyecto decidió aceptarlo**: la pantalla del código ya avisa explícitamente que puede caer en spam —ese texto se escribió por este motivo— y el cliente no lo considera un bloqueo. Baja de `Medium` a `Low` porque deja de ser trabajo pendiente y pasa a ser una condición conocida. Si algún día molesta, las palancas son endurecer DMARC a `p=quarantine` una vez confirmada la alineación, o acumular volumen con quejas bajas. |
| 2026-08-11 | ~~Medium~~ | ~~**El correo de acceso cae en *Correo no deseado* en Outlook/Hotmail, y SC-004 exige bandeja de entrada en dos proveedores distintos.**~~ **Reemplazada por la fila de arriba al cerrarse la medición.** Medido el 2026-08-11 con un envío de prueba desde `info@send.flashurbano.uy`: **Gmail lo puso en Recibidos** en menos de un minuto —lo que demuestra que DKIM y SPF están bien, porque Gmail no es permisivo con correo no autenticado de un dominio nuevo— y **Hotmail lo mandó a Junk**. No es un defecto de configuración: el dominio tiene horas de vida y cero reputación acumulada, y Microsoft es el más severo con remitentes nuevos. Dos salvedades antes de tratarlo como un problema de fondo: el mensaje de prueba era **una sola línea suelta sin estructura**, que puntúa peor que el correo real del código —que lleva texto y HTML, asunto transaccional y remitente consistente—, y las IP de envío son de Resend/SES, así que **no se pueden registrar en SNDS ni JMRP de Microsoft**: la reputación que importa es la del dominio, y se construye mandando. Próximo paso, que es el que decide: **volver a medirlo con el correo real del código** cuando T055 esté desplegado, no con la prueba. Si el real también cae en Junk, las opciones son endurecer DMARC a `p=quarantine` una vez confirmada la alineación, acumular volumen con quejas bajas, o aceptar que en Microsoft el código se busca en Spam — que para un login es una experiencia mala pero no un bloqueo. |
| 2026-08-11 | Medium | **El índice de calles tiene 50 grupos de nombres homónimos, y `callePorNombre` devuelve el primero — que puede ser el equivocado.** `web/public/calles-mvd.json` trae **100 calles en 50 grupos** cuyos nombres se normalizan al mismo texto, casi siempre la misma calle escrita con y sin tilde en el dato de origen: `Vicente Yañez Pinzón` (id 255, **1** esquina) y `Vicente Yáñez Pinzón` (id 3331, **24**) son dos entradas distintas del índice. `callePorNombre` (`web/lib/direcciones.ts:223`) hace `find()`, o sea se queda con la primera, y no tiene forma de saber cuál de las dos quiso decir quien pregunta. **Se detectó en producción de hecho**, no en teoría: la pantalla de perfil no pudo reconstruir una dirección guardada perfectamente válida —Doctor Martín Berinduague y Vicente Yáñez Pinzón— porque el par que armaba con la primera coincidencia cruzaba en cero lugares, y le decía al cliente "no pudimos ubicarla en el mapa". **El perfil ya no depende de eso**: `formulario-perfil.tsx` prueba todas las combinaciones de homónimos y desempata con el punto guardado. Lo que queda abierto es la función, que es una trampa armada para el próximo que la use — hoy sólo la llaman las pruebas. Y queda la causa de fondo: el índice **no debería tener dos entradas para la misma calle**. Arreglos, de menor a mayor: que `callePorNombre` devuelva todas las coincidencias y obligue a quien llama a decidir (cambia su firma, `web/lib/` fuera del `covers:` de `006`); o unificar los homónimos al generar el índice en `design-source/build-calles.js`, que es donde está el defecto de verdad — **con cuidado, porque fusionar dos calles fusiona sus esquinas y eso toca el dato que decide la plata**. |
| 2026-08-11 | Low | **Una dirección de retiro guardada no se puede borrar, sólo reemplazar.** `PUT /yo` trata la dirección ausente como "conservá la guardada" (COALESCE), que es lo que evita que la pantalla de alta —sólo nombre y teléfono— le pise la dirección a quien ya la tenía. El efecto colateral es que **no hay ningún valor que signifique "borrala"**: `null` conserva igual. Quien se mude puede escribir la nueva, así que no hay nadie bloqueado; quien quiera dejar de tenerla guardada, no puede. Se suma al ítem de borrado de cuenta que `006` ya declara como deuda en las Assumptions del spec, y conviene resolverlos juntos: los dos son "el usuario quiere que dejemos de tener un dato suyo". Arreglo: distinguir ausente de `null` explícito en `pedidoActualizarYo` — hoy ambos llegan como puntero nil. |
| 2026-08-11 | Low | **Con una dirección precargada, el campo *Esquina* queda deshabilitado hasta volver a elegir la calle.** `BloqueDireccion` guarda en estado interno cuál es la `Calle` elegida, y ese estado **no se puede rehidratar desde afuera**: la pantalla de perfil reconstruye la `Esquina` resuelta y el punto —lo que hace andar el mapa y el arrastre— pero el componente sigue creyendo que nadie eligió calle, así que deshabilita el campo de la esquina con el mensaje *"Elegí primero la calle."*. Para cambiar sólo la esquina de una dirección guardada hay que volver a tipear la calle. **No pierde datos ni miente**, y el camino de salida está a la vista, pero es un paso de más justo en la pantalla que existe para ahorrarlos. No se arregló porque T063 dice explícitamente reutilizar `bloque-direccion.tsx` **sin modificarlo**, y el archivo está fuera del `covers:` de `006`. Arreglo: aceptar una `Calle`/`Esquina` inicial por props, o mover ese estado interno a `EstadoDireccion`, que es donde ya vive el resto. |
| 2026-08-11 | **High** | **`backend/.env` sólo está ignorado en la rama `backend-auth`, así que en cualquier otra rama un `git add -A` lo deja a un commit de un repo público.** Pasó el 2026-08-11: trabajando en `dominio-propio` —que no tiene `backend/.gitignore`, porque ese archivo lo crea `006`— el `.env` con la contraseña de la base local, el Client ID de Google y el mail administrador quedó **staged**. Lo frenó el sensor de cobertura, y **por casualidad**: lo detuvo porque `backend/` no está en el `covers:` de `009`, no porque supiera que era un secreto. En `backend-auth` esa red no existe —ahí `backend/` sí está cubierto— y lo único que protege es el `.gitignore` de ese directorio. Nunca llegó a ningún commit (`git log --all -- backend/.env` vacío), pero la protección depende de en qué rama se esté parado, que es exactamente la clase de garantía que no sirve. Contradice FR-028 ("el repo es público: ningún secreto en el árbol"). Arreglo: mover el patrón `.env*` al `.gitignore` de la **raíz**, que aplica en todas las ramas por igual — una línea, hoy fuera del `covers:` de los dos planes vivos. Arreglo complementario y más robusto: un hook de pre-commit que rechace archivos que parezcan secretos por nombre, **independiente de `covers:`** — el sensor actual protege el alcance del plan, no el contenido. |
| 2026-08-10 | Medium | **Que cotizar no dispare una llamada de red ahora depende de un `if` sin prueba automática.** Hasta T040, la promesa de FR-001 —cotizar funciona con el servicio caído— la sostenía una guarda estática: `web/lib/cotizar-abierto.test.ts` recorre el grafo de imports desde `pedido-form.tsx` y falla si llega a `lib/api.ts` o `lib/sesion.ts`. T040 montó el proveedor de sesión en `app/layout.tsx`, o sea **arriba de `/pedido`**, y ese proveedor sí importa `lib/api.ts`. El layout no figura entre las entradas de esa prueba y no puede figurar: la pondría en rojo por un import que tiene que existir. Lo que evita la llamada es una propiedad de comportamiento —sin credencial guardada el proveedor no pide nada— que vive en un `if` de `components/sesion/proveedor-sesion.tsx`. **Si alguien saca ese `if`, ninguna prueba se entera**, y el síntoma aparece recién el día que el servicio se cae: un desconocido entra a cotizar y la pantalla espera diez segundos antes de rendirse. Hoy sólo lo cubre T025, que es manual. Arreglo: mover `web/` a un entorno de pruebas con DOM (jsdom o `vitest-browser`) y montar el proveedor con `fetch` espiado y `localStorage` vacío, afirmando cero llamadas — es la prueba de tres líneas que hoy no se puede escribir. Toca `web/vitest.config.ts`, fuera del `covers:` de `006`. |
| 2026-08-10 | Medium | **La CSP del sitio lleva `'unsafe-inline'` en `script-src`, así que no impide que un XSS se ejecute — sólo que se lleve la credencial.** El research D4 justificó guardar la credencial de sesión en `localStorage` diciendo que "sumar una CSP estricta cierra el vector más probable". Al implementar T040 se descubrió que **no se puede**: GitHub Pages no permite encabezados HTTP (queda sólo `<meta http-equiv>`, que además no soporta `frame-ancestors` ni `report-uri`), y la vía que documenta Next para una CSP estricta usa *nonces*, que exigen renderizado dinámico — imposible en un export estático. Encima Next emite **ocho scripts inline** en el HTML publicado; un `script-src 'self'` los bloquea y el sitio no hidrata. Lo que la CSP sí hace es acotar `connect-src` al propio origen, Google y el servicio, así que un script inyectado puede **leer** la credencial y no **mandarla** a un servidor propio: cierra la exfiltración, no la ejecución. **Está corregido en el research (D10) y no oculto**, pero la defensa es más floja que la que se prometió al elegir `localStorage`. Arreglo: calcular los hashes SHA-256 de los scripts inline por build y reescribir el `<meta>` en cada HTML del export — es la única forma de una CSP estricta acá; requiere post-proceso del `out/`, cambia con cada build, y toca archivos fuera del `covers:` de `006`. Alternativa mayor: mover el sitio a un hosting que permita encabezados, lo que reabre el ADR `backend-persistence-stack`. |
| 2026-08-10 | Medium | **Dos features en dos ramas hacen que el `status:` de los planes se pise en cada merge, y el merge no avisa.** El harness admite un plan `active` por árbol de trabajo. Con `006` ejecutándose en `backend-auth` y `009` en `dominio-propio`, cada rama tuvo que demotar el plan de la otra. Al mergear `master` en `backend-auth`, git juntó las dos ediciones **sin conflicto** —tocaban líneas distintas del frontmatter— y dejó `006` en `draft` y `009` en `active`, o sea al revés de lo que esa rama necesita. **Que no haya conflicto es lo peligroso**: un conflicto obliga a decidir, un auto-merge limpio no, y el síntoma aparece recién cuando el sensor rebota un commit de código con un mensaje que habla de `covers:` y no de esto. Se detectó porque se revisó a mano después de mergear, no porque algo avisara. Arreglos posibles, de menor a mayor: correr cada feature en su **propio git worktree**, que es literalmente lo que la regla dice (`por árbol de trabajo`) y hace desaparecer el problema; o que `check_plan_coverage.py --doctor` avise cuando el plan activo no coincide con el `feature_directory` de `.specify/feature.json`. |
| 2026-08-09 | Medium | **Justo afuera del índice de calles, el sitio contesta con un callejón sin salida en vez de con la respuesta comercial.** `design-source/build-calles.js` recorta el índice al área de las cinco zonas más ~2 km de margen, y el corte es una línea dura: al norte termina en lat `-34.7462`, partiendo La Paz al medio (141 calles adentro, el resto afuera). Quien cae del lado de adentro escribe su calle, marca el punto y lee *"Ese punto queda fuera de nuestra zona de cobertura — escribinos y vemos cómo darte una mano"*, que es una respuesta de negocio y deja una puerta abierta. Quien cae 200 m más al norte lee *"No hay calles que coincidan."* y no llega a ningún lado: parece un defecto del sitio, no una decisión de cobertura. **La diferencia entre las dos experiencias la decide el margen del script que genera el índice, no el negocio.** Verificado a mano el 2026-08-09 sobre La Paz; adentro de las cinco zonas cobradas no hay agujeros (Zona 5 / Ciudad de la Costa tiene 717 calles y 2264 esquinas, así que esto **no** afecta a ningún cliente que pueda pedir). Arreglos posibles, de menor a mayor: que el estado vacío del autocompletado diga que la dirección está fuera del área de servicio y ofrezca el contacto directo —arregla el mensaje sin tocar el dato—, o ampliar el margen del índice, que agranda el JSON que descarga todo el mundo. Fuera del `covers:` de `006`. |
| 2026-08-09 | Medium | **El plan Hobby de Railway puede no cubrir uso comercial, y Flash Urbano es el negocio de un cliente.** Los Términos de Railway incluyen la cláusula *"You will only use the Services for your own internal, personal, non-commercial use, and not on behalf of or for the benefit of any third party"*, y la documentación posiciona Hobby como *"for indie hackers... personal projects"* frente a Pro *"for professional developers and their teams shipping to production"*. La página de *fair use* **no** menciona uso comercial y la de planes **no** lo prohíbe explícitamente, así que la ambigüedad es real y no se pudo resolver leyendo la documentación pública. Se eligió Hobby (USD 5/mes + excedente de uso, ~USD 6-7/mes reales para esta carga) con la duda abierta y a sabiendas. **Importa el doble porque el ADR `backend-persistence-stack` descartó Vercel exactamente por esto** —su tier gratuito es no comercial— y comparó "Vercel Pro contra el tier pago de Railway"; si el tier pago aplicable resulta ser Pro a USD 20/mes, esa comparación es un empate y no una victoria. No invalida la elección de Railway (PostGIS y la experiencia previa del dueño siguen decidiendo), pero sí el argumento económico del ADR. Arreglo: preguntarle a Railway por escrito —su propia página de fair use dice *"If you are unsure whether your use case is allowed, ask us before deploying"*— y, según la respuesta, quedarse en Hobby o subir a Pro y corregir el párrafo económico del ADR. |
| 2026-08-09 | Low | **El servicio exige las seis variables obligatorias para servir `GET /salud`.** `config.Cargar()` falla al arrancar si falta cualquiera (FR-028), incluidas `CORREO_API_KEY` y `GOOGLE_CLIENT_ID`, que en la Fase 2 todavía no las usa nadie. La consecuencia práctica es que el primer despliegue necesita valores de relleno para variables de funcionalidad que no existe. Es el precio deliberado de fallar temprano y por ahora es aceptable. Si molesta al avanzar, el arreglo es separar lo obligatorio-al-arrancar de lo obligatorio-al-usarse, validando cada credencial cuando su camino se activa en vez de en el boot. |
| 2026-08-06 | Medium | **`web/lib/zonas.ts` afirma algo que dejó de ser cierto, y no se puede corregir a mano.** El comentario sobre `ZONAS` dice que el orden de la lista "es normativo: resolverZona() lo recorre tal cual y devuelve el primer match, y eso es lo que hace determinista la resolucion sobre un borde compartido". Desde `004` el desempate lo decide **el precio**, no la posición en la lista, así que el comentario le miente a quien lo lea — y encima le sugiere que el orden es load-bearing cuando ya no lo es. `zonas.ts` es **archivo generado**: el texto vive en `web/design-source/build-zonas.js` y arreglarlo exige regenerar. No se hizo en `004` porque `web/design-source/` no está en su `covers:` y regenerar toca el dato que decide la plata — eso merece su propio plan, no un arreglo de paso. Arreglo: corregir el encabezado en el generador y regenerar verificando que la geometría salga byte a byte igual. |
| 2026-08-06 | Medium | **El copy de `/pedido` explica mal cómo se usa la pantalla.** `web/app/pedido/page.tsx:16` dice "marcá en el mapa desde dónde retiramos el paquete". Eso dejó de ser cierto en `003`: el punto sale del cruce de calles y sólo puede arrastrarse dentro de la cuadra declarada — de hecho, marcar en el mapa es justamente lo que `003` prohibió, porque un punto libre hace el precio manipulable. Detectado durante `004`; el dueño del repo decidió el 2026-08-06 dejarlo afuera de ese feature y anotarlo, así que `web/app/pedido/` quedó deliberadamente fuera de su `covers:`. **Sube de prioridad porque `004` sacó el `noindex`**: el sitio ya es indexable y esta frase es lo primero que lee alguien que llega desde un buscador. |
| 2026-08-06 | Low | **Las claves de error del formulario no están tipadas contra los campos.** En `web/components/pedido-form.tsx`, `validate()` devuelve `Record<string, string>` y el render lee claves sueltas: nada obliga a que coincidan. Es lo que dejó invisible el error de `recieverName` durante dos features (ver la fila resuelta correspondiente). `004` eliminó ese caso concreto, pero no la clase de defecto: quedan ~15 claves donde el mismo error puede volver a colarse sin que el compilador diga nada. Arreglo: derivar el tipo de las claves de `FormState` más las sintéticas (`ubicacionRetiro`, `entregaCalle`…). No se hizo en `004` porque es un refactor del formulario entero y `AGENTS.md` prohíbe la limpieza oportunista fuera de los pasos del plan. |
| 2026-08-06 | Medium | **`SECURITY.md` describe un repo sin backend, y eso deja de ser cierto.** Hasta `004` el sitio era estático: sin servidor, sin credenciales, sin frontera de confianza. El ADR `backend-persistence-stack` introduce servidor, base de datos y un secreto real, en un repo **público**. Hay que revisar `SECURITY.md` entero —secretos, auth, trust boundaries— al implementar el backend, no después. No se hizo en el ADR porque un ADR decide, no documenta postura de seguridad. |
| 2026-08-04 | High | **Las esquinas del índice de calles no tienen verificación automática.** `SC-002` de `003` pedía un fixture de al menos 30 esquinas conocidas con coordenadas confirmadas a mano contra un mapa real, más un test con tolerancia de 30 m (tareas T012 y T013). **No se hizo**: el dueño del repo validó el flujo a mano y decidió cerrar el feature igual. Lo que queda sin red es esto: `web/public/calles-mvd.json` se regenera con `design-source/build-calles.js`, y si una regeneración futura desplaza las esquinas —por un cambio en el dato de origen, en el colapso de calzadas de 60 m o en el encadenado de polilíneas— **el precio sale mal y ningún test lo detecta**. Durante la investigación, dos de cinco esquinas que se daban por ciertas de memoria resultaron estar mal, así que las coordenadas del fixture las tiene que confirmar una persona: generarlas desde el mismo índice haría el test tautológico. |
| 2026-08-04 | Medium | **`003` eliminó el botón "Usar mi ubicación" del formulario, y eso no estaba en el plan.** Ese botón colocaba el punto de retiro desde el GPS, o sea en cualquier lado. Desde `003` el punto sale del cruce de calles y solo puede moverse dentro de la cuadra declarada (FR-014), que es el control que impide arrastrar el pin hacia la zona más barata. Un punto de GPS entra sin pasar por ese control, así que dejarlo habría abierto de nuevo el agujero que el feature cierra. Se quitó en vez de dejarlo roto. **Lo que se perdió es real**: era un atajo cómodo para quien pide desde su propia casa. La reposición razonable es usar el GPS para *sugerir la esquina más cercana* y que el flujo siga por el camino normal, pero eso es funcionalidad nueva y necesita su propio spec. |
| 2026-08-02 | Low | `.github/workflows/deploy-pages.yml` todavía dispara el deploy a producción desde la rama `mvp-flash-urbano`, que ya está **completamente mergeada en `master`** y quedó de la primera etapa. Un push a esa rama muerta publicaría el sitio. Sacarla del `on.push.branches` y dejar solo `master`. No se hizo al detectarlo porque `.github/` no está cubierto por ningún plan ni exento del sensor de cobertura, así que necesita un plan propio (o entrar de paso en el próximo que toque CI). |
| 2026-08-02 | Low | `web/vitest.config.ts` dispara un warning de Vite: usa sintaxis ESM en un archivo que se carga como CommonJS. Es un aviso sobre un default **futuro** de Vite, no un error, y `npm test` funciona. El arreglo documentado es renombrar a `.mts` o poner `"type": "module"` en `package.json`. No se hizo en `002` porque `.mts` caía fuera del `covers:` de ese plan y el warning es cosmético. Resolver la próxima vez que se toque el toolchain. |
| 2026-08-01 | Low | `web/package.json` usa `overrides` para forzar `sharp@^0.35.3` y `postcss@^8.5.25` por encima de lo que declara `next@16.2.12` (`sharp ^0.34.5`, `postcss` pineado en `8.4.31`), que arrastraban advisories de libvips y de PostCSS. `npm audit` queda en 0. Revisar y **quitar los overrides** cuando Next publique una versión que ya traiga esas deps parcheadas — mantenerlos de más nos deja resolviendo versiones que Next no testeó. Verificar con `npm audit` + `npm run build` + una request a `/_next/image` (es lo que ejercita sharp). |

## Resolved

| Date identified | Severity | Description | Resolution |
|---|---|---|---|
| 2026-08-11 | Medium | **El perfil guarda cuatro campos de la dirección y el formulario de pedido pide seis, así que `apto` y `cooperativa` se van a seguir tipeando en cada envío.** FR-019a define la dirección guardada como `calle`, `esquina`, `numero` y `punto`; `web/components/bloque-direccion.tsx` —que el perfil reutiliza tal cual, por T063— también pide *Apto* y *¿Es una cooperativa?*, y esos dos no viajan en el `PUT /yo` ni existen en la tabla. La pantalla **lo dice** en vez de dejar que se descubra al volver ("El apto y si es una cooperativa se piden al hacer cada pedido"), así que no hay pérdida silenciosa, pero sí una promesa a medias: el valor del perfil es no volver a escribir la dirección, y quien vive en un apartamento la sigue escribiendo en parte. Se detectó al construir la pantalla, no al especificarla. Arreglo: sumar las dos columnas a `usuarios`, al `pedidoRetiro` y a la `VistaRetiro` — todo dentro del `covers:` de `006`, pero **fuera de los pasos de T063/T064**, así que entra como decisión de producto y no como limpieza de paso. Conviene resolverlo **antes de `007`**, que es quien precarga el formulario y donde el hueco se vuelve visible. | **Resuelto el mismo día.** El dueño del proyecto decidió que los dos campos entran al perfil —"es parte de la info del cliente"— y se implementó en la Fase 10 de `006` (T079–T083): migración `0002`, repositorio, handlers, vista y formulario. Van como **nulables**, para que "no lo declaró" siga siendo distinguible de "declaró que no". |
| 2026-08-04 | Medium | **La dirección del backend está decidida pero no escrita como ADR.** Acordado con el dueño del repo el 2026-08-04: el backend **va a existir**, pero recién cuando entren usuarios y persistencia de pedidos — no antes, porque el sitio es estático en GitHub Pages y sumar servidor mata ese modelo sin necesidad. Cuando llegue: **Postgres con PostGIS**, porque la app Android tiene que ordenar entregas por cercanía. Precisión que conviene no perder: **PostGIS da distancia, no ruteo** — recorrido y tiempo de viaje son `pgRouting` o un motor aparte, y consumen la misma red de calles que ya construyó `003`. El lenguaje quedó abierto (Go vs. TypeScript en el mismo Next). No se escribió ADR **a propósito**: sin restricciones reales de hosting, presupuesto y auth sería inventarlas. **El ADR se escribe al abrir el spec del backend**, y esta fila existe para que esa discusión no arranque de cero. | Pagada el 2026-08-06 por [ADR backend-persistence-stack](decisions/backend-persistence-stack.md). Cierra lenguaje (**TypeScript en el mismo Next**, no Go — con el motivo real de Go registrado como lo que era), hosting (**Vercel**, y GitHub Pages se retira), motor (**Neon Postgres con PostGIS** desde el día uno, con el punto guardado como geometría). Mantiene intactas las dos precisiones que esta fila protegía: PostGIS no rutea, y el lookup de zonas **no** se muda a la base — el precio no puede depender de que una request salga bien. Lo que el ADR deja deliberadamente sin decidir es **auth**, porque la nota del cliente en el doc admite dos lecturas y adivinar sería inventar un requisito. |
| 2026-08-04 | High | **Decisión de negocio pendiente del cliente: qué zona paga una dirección que cae sobre la avenida que es límite de zona.** Los límites los definió el cliente por nombre de calle, así que Bulevar Artigas, Avenida Italia y 8 de Octubre son borde de zona *y* tienen cientos de esquinas encima. Cuando `003` autocoloque el punto en un cruce de esos, el punto cae exactamente sobre el límite y quien decide el precio termina siendo el desempate de `web/lib/zona-lookup.ts` — que está testeado y documentado, pero es una convención interna, no una respuesta del negocio. No es un caso raro: es una franja entera de direcciones reales. **Bloquea el precio de esas direcciones en `003`, no el resto de la feature.** Preguntado al cliente el 2026-08-04, sin respuesta todavía. | Resuelto en `004` (T015, T016). El cliente contestó en su doc de relevamiento: paga **la zona de menor costo**. `resolverZona()` pasó a elegir por precio en vez de por posición en la lista, con el id más bajo como desempate cuando dos comparten precio, y la lógica toma la lista de zonas por parámetro para poder probarla contra un caso donde la más barata **no** es la primera — con las cinco zonas reales eso es imposible, porque sus precios resultan crecientes por id y cualquier implementación pasaría. **Corrección de un dato que esta fila afirmaba mal**: no es "una franja entera". Medido sobre una grilla de ~50 m en toda el área de servicio, sólo **16 puntos de 551.601** son reclamados por dos zonas, más 10 vértices y 26 puntos medios de lado — el ray casting usa desigualdad estricta y eso manda casi todo el borde a una sola zona. El caso es real y vale la regla, pero es raro. De paso se descubrió que el test que decía cubrir "un borde compartido" usaba un punto que reclamaba **una sola** zona. |
| 2026-08-02 | Medium | En `web/components/pedido-form.tsx`, el mensaje de error del nombre de quien recibe **nunca se muestra**: `validate()` escribe la clave `recieverName` (con la `i` traspuesta) y el render lee `errors.receiverName`. El campo sí bloquea el envío, así que no se cuela un pedido incompleto, pero el usuario no ve por qué. Detectado al planificar `specs/002-mapa-zonas-precio/`; **no se arregló ahí** porque queda fuera de los pasos de ese plan (`AGENTS.md`: nada de limpieza oportunista). Arreglo: unificar la clave en las dos puntas. | Resuelto en `004` (T010), aunque no arreglando la clave: **el campo desapareció**. El cliente pidió sacar nombre y cédula de quien recibe —se capturan en la app Android al entregar— y poner un teléfono en su lugar. El campo nuevo usa `receiverPhone`, escrita igual en `validate()` y en el render, y `quickstart.md` tiene un paso manual dedicado a confirmar que su error **se ve**. La clase de defecto sigue viva: ver la fila `Low` del 2026-08-06 sobre tipar las claves de error. |
| 2026-08-02 | High | **El mapa se dibuja por encima de la navbar en mobile**, en `/pedido` y en `/sobre-nosotros` (es el mismo componente, `web/components/mapa-zonas.tsx`). Al scrollear, el mapa tapa el header sticky y el logo. Causa: la navbar es `z-50` (`web/components/nav-bar.tsx`) y el CSS propio de Leaflet declara `z-index: 400` en `.leaflet-pane` y `1000` en `.leaflet-top`/`.leaflet-bottom`, así que le gana siempre. **Subir la navbar a `z-[1001]` es la salida equivocada**: arranca una carrera de z-index contra una librería que ya usa 1000. Lo correcto es **encerrar a Leaflet en su propio contexto de apilado** — `isolation: isolate` (o `position: relative; z-index: 0`) en el contenedor del mapa — para que sus z-index internos no escapen al resto de la página. Acordado como primera tarea del 2026-08-03. | Resuelto en `003` (T016): el contenedor de `web/components/mapa-zonas.tsx` lleva `isolation: isolate`, que encierra los z-index de Leaflet en su propio contexto de apilado. No se tocó la navbar, así que no quedó ninguna carrera de z-index abierta. Verificado a mano en viewport de teléfono por el dueño del repo el 2026-08-04. |
| 2026-08-02 | Medium | **Se pide la misma información dos veces en el formulario de pedido**: calle + número + esquina escritos, y además marcar el punto en el mapa. Son el mismo dato, y eso choca con el Principio IV (mínimo tipeo). Lo detectó el dueño del repo probando el MVP. La solución que quiere es geocodificación en ambos sentidos (dirección → punto para el precio; punto → calle y número aproximado para el timbre). Va a `specs/003-*` con ADR propio, porque **reversa** la decisión de `002` de no depender de servicios externos. Alternativa a evaluar que evitaría el servicio externo: **cruce de calles** — el formulario ya pide calle y esquina, y con la geometría de calles servida desde el propio sitio se puede calcular la intersección con precisión de cuadra, que alcanza porque los límites de zona son avenidas. Lo que ninguna de las dos vías da con precisión es el número de puerta. | Resuelto en `003`: el retiro se carga con calle y esquina autocompletadas contra `web/public/calles-mvd.json`, y el punto sale del cruce en vez de marcarse aparte. La entrega también quedó con autocompletado, agregado el 2026-08-04 tras probar el formulario, pero sin mapa ni punto (FR-007a) y sin bloquear si la calle no está en el índice (FR-007b): no incide en el precio y la ubica la app Android. |
| 2026-08-02 | High | El sensor de cobertura del harness crasheaba con `exit 1` en Windows, o sea que **rechazaba todo commit**: `subprocess.run(text=True)` sin `encoding` decodifica con el codec del sistema (cp1252), y cualquier plan con acentos lo hacía explotar. Los artefactos de este repo están en español, así que afectaba a todos. | Arreglado en `scripts/harness/check_plan_coverage.py`: se fuerza `encoding="utf-8", errors="replace"` en las tres llamadas a `subprocess.run`. `--selftest` pasa sus 8 casos. Detectado al intentar el primer commit de `002`. **Es un bug del scaffold `init-harness-speckit`, no solo de este clone** — conviene reportarlo aguas arriba. |
| 2026-08-01 | Medium | Los límites de zona del mapa de `/sobre-nosotros` son una **interpretación**, no un dato validado: las líneas que trazó el cliente son polilíneas abiertas, y para pintar las zonas hubo que cerrar los extremos (ver `web/design-source/README.md`). Las zonas 3 y 5 se cortan en el borde de la captura. Como cada zona define un precio, **pedirle al cliente que confirme los límites** antes de que el mapa se use como referencia de cobro. Regenerable con `node design-source/build-map.js`. | Retirada por `specs/002-mapa-zonas-precio/`: el cliente definió los límites por nombre de calle, los validó, y las zonas pasaron a ser geometría real en `web/lib/zonas.ts`. Los límites ya no son una interpretación. |
| 2026-08-01 | Low | La base del mapa de zonas es una captura de Google Maps modificada y publicada sin atribución en un sitio comercial, lo que no cumple los términos de Google. Sirve para el MVP; si el mapa queda a largo plazo, rehacerlo sobre tiles de OpenStreetMap (permiten uso comercial citando la fuente). | Retirada por `specs/002-mapa-zonas-precio/`: el mapa se rehizo sobre mosaicos de OpenStreetMap con la atribución visible. Precisión que quedó documentada en `research.md` D2: ODbL cubre los *datos*; los *servidores* de mosaicos tienen su propia política de uso, orientada a volumen bajo. |
