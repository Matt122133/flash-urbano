<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Tres hosts nombrados, y ninguno mas. NO se habilita texto plano en
         general: si `cleartextTrafficPermitted` estuviera en `base-config`,
         cualquier destino quedaria habilitado, y este archivo existe
         justamente para que eso no pase.

         - `10.0.2.2` es como el emulador ve el `localhost` de la maquina.
         - `localhost` y `127.0.0.1` son para probar en un TELEFONO de verdad
           con `adb reverse tcp:8080 tcp:8080`, que hace salir el localhost del
           telefono por el cable (nivel 3 del quickstart de `018`). Es preferible
           a poner la IP de LAN: no depende del firewall ni de una direccion que
           cambia sola, y sobre todo **no deja anotada en el repo una IP de la
           red de nadie**.

         Todo esto vive en `src/debug/` y por eso no puede llegar al APK que se
         instala en el telefono de Diego (research D5 de `012`). -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
